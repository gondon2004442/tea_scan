{
  "name": "tea-scan-ai-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker AI proxy for Tea Scan (Gongfu search + quiz).",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.86.0"
  }
}
